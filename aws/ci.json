{
  "Comment": "pyx-lambda ASL for pipeline \"ci\" (generated by pyxlambda compiler — DO NOT EDIT). TF provisioning of the Step Functions state machine, Lambda, Fargate, and CodeBuild resources is handled by terraform-provider-pyxcloud, not by this compiler.",
  "StartAt": "check-and-test/0/checkout",
  "States": {
    "check-and-test/0/checkout": {
      "Type": "Task",
      "Resource": "arn:aws:states:::lambda:invoke",
      "Parameters": {
        "FunctionName": "${PyxRunnerLambdaArn}",
        "Payload": {
          "step": "checkout"
        }
      },
      "Next": "check-and-test/1/setup go"
    },
    "check-and-test/1/setup go": {
      "Type": "Task",
      "Resource": "arn:aws:states:::lambda:invoke",
      "Parameters": {
        "FunctionName": "${PyxRunnerLambdaArn}",
        "Payload": {
          "step": "setup go",
          "with": {
            "tool": "go",
            "version": "1.25"
          }
        }
      },
      "Next": "check-and-test/2/build/vet/test"
    },
    "check-and-test/2/build/vet/test": {
      "Type": "Task",
      "Resource": "arn:aws:states:::lambda:invoke",
      "Parameters": {
        "FunctionName": "${PyxRunnerLambdaArn}",
        "Payload": {
          "step": "build/vet/test",
          "run": "go vet ./... \u0026\u0026 go test ./..."
        }
      },
      "End": true
    }
  }
}